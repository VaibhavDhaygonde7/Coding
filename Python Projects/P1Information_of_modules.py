write 'pip install (here the module name without brackets)'

install the modules before using them

some modules are already avaible such as random

write 'python' to start the python interpreter in the terminal and write 'exit()' to exit the python the python interpreter

pip is the package manager of python